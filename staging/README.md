# staging/ — hand-off bundles

This directory is **not part of the Agent-Foundation proto module** (`buf.yaml`
only covers `proto/`). It holds scaffolds for *other* AGenNext repos that were
generated here because this session only has write access to
`agennext/agent-foundation`.

**These folders are meant to be moved out**, each into its own repo, then
deleted from here. They are starting points, not the final home.

| Folder | Destination repo |
| --- | --- |
| `agent-platform/` | `AGenNext/Agent-Platform` |
